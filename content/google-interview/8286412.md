---
post_id: 8286412
title: "Google Application Engineer Interview Questions"
date: 2026-05-24T05:00:21.413013
difficulty: medium
tags: ["String Manipulation", "Matrix", "Arrays", "Combinatorics", "Hash Table", "Sliding Window", "Anagram"]
---

# Google Application Engineer Interview Questions

**2026-05-24** · [LeetCode](https://leetcode.com/discuss/post/8286412/google-application-engineer-upcoming-cod-99aa/)

A collection of questions frequently asked in Google's Application Engineer interviews.

## Questions

### String Manipulation · Medium
Given a smaller string `s` and a bigger string `b`, design an algorithm to find all permutations of the shorter string within the longer one. Print the location of each permutation.

**Approach:** This is a classic sliding window problem. Use a hash map or an array to store the character frequencies of the shorter string `s`. Slide a window of size `len(s)` over `b`, maintaining a frequency map of the current window and comparing it to the map of `s`.

### Matrix Manipulation · Medium
You are given an `n x n` 2D matrix representing an image. Write a function to rotate the image by 90 degrees (clockwise) in-place.

**Approach:** The standard in-place solution involves two steps: first, transpose the matrix (swap `matrix[i][j]` with `matrix[j][i]`), and then reverse each row.

### Combinatorics · Medium
Given an integer array `nums` of unique elements, return all possible subsets (the power set).

**Approach:** This can be solved using backtracking (recursion). For each element, there are two choices: either include it in the current subset or not. Explore both paths to generate all possibilities.

## Similar Problems

- [Find All Anagrams in a String](https://leetcode.com/problems/find-all-anagrams-in-a-string/) - Directly related to the permutation finding problem, using the same sliding window technique.
  ```python
  def findAnagrams(s: str, p: str) -> List[int]:
      pass
  ```
- [Subsets II](https://leetcode.com/problems/subsets-ii/) - A variation of the subsets problem where the input array may contain duplicates.
  ```python
  def subsetsWithDup(nums: List[int]) -> List[List[int]]:
      pass
  ```
